# Frontend package notes

The live AgriPulse dashboard lives in this folder.

For full stack setup, architecture, and `docker compose` instructions, see the
repository root [README.md](../README.md).

### Local UI only

```bash
npm install
npm run dev
```

Requires the API at `http://localhost:8000` (Compose `backend` service).
